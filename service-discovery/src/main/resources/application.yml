spring:
  application:
    name: 'service-discovery'
  cloud:
    config:
      uri: 'config-server'
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: ${PORT:8761}
