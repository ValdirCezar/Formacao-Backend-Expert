version: "3.8"

services:
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: 'rabbitmq'
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - ~/.docker-conf/rabbitmq/data/:/var/lib/rabbitmq/
      - ~/.docker-conf/rabbitmq/log/:/var/log/rabbitmq
    networks:
      - helpdesk

  service-discovery:
    container_name: 'service-discovery'
    build:
      context: ./service-discovery
      dockerfile: Dockerfile
    ports:
      - "8761:8761"
    expose:
      - 8761
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://service-discovery:8761/actuator/health" ]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - helpdesk

#  api-gateway:
#    image: valdircezar/helpdesk-api-gateway
#    container_name: 'api-gateway'
#    environment:
#      - SERVER_PORT=8765
#      - EUREKA_URI=http://service-discovery:8761/eureka
#    ports:
#      - "8765:8765"
#    expose:
#      - 8765
#    depends_on:
#      - service-discovery
#    networks:
#      - helpdesk
#
#  config-server:
#    image: valdircezar/helpdesk-config-server
#    container_name: 'config-server'
#    environment:
#      - CONFIG_SERVER_URI=https://github.com/ValdirCezar/formacao-backend-expert-configs
#      - CONFIG_SERVER_USERNAME=valdir_cezar
#      - CONFIG_SERVER_PASSWORD=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
#      - EUREKA_URI=http://service-discovery:8761/eureka
#      - PORT=8888
#    ports:
#      - "8888:8888"
#    expose:
#      - 8888
#    depends_on:
#      - service-discovery
#    networks:
#      - helpdesk

networks:
  helpdesk:
    driver: bridge